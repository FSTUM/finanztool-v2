apiVersion: v1
kind: Service
metadata:
    name: finanztool
    labels:
        app: finanztool
spec:
    type: NodePort
    selector:
        app: finanztool
    ports:
        - port: 8000
          targetPort: 8000
